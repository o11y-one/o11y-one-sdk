{
  "name": "@o11y-one/sdk-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root. Not published — see packages/gen-ts-agentic and packages/sdk-ts.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" run build",
    "typecheck": "pnpm -r --filter \"./packages/**\" run typecheck",
    "test": "pnpm -r --filter \"./packages/**\" run test"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
